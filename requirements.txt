# Core computer vision and image processing
# Use the headless build on servers where OpenGL is not available
# (prevents libGL.so.1 import errors on many cloud hosts)
opencv-python-headless>=4.9.0
Pillow>=10.3.0
numpy>=1.26.0
ultralytics>=8.1.0

# Progress bars
tqdm>=4.66.0

# Data handling and utilities
scipy>=1.13.0

# YAML support (camera parameters output)
PyYAML>=6.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Frontend UI
streamlit>=1.30.0

